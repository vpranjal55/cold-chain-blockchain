{
  "name": "coldchain-hardhat",
  "version": "1.0.0",
  "description": "Hardhat project for ColdChainAgreement.sol — local chain, deploy script, ABI export for the Python oracle bridge.",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  },
  "license": "MIT"
}
